apiVersion: v1
kind: Service
metadata:
  name: workshop-service
  labels:
    app: workshop-service
spec:
  selector:
    app: workshop-service
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
  
---

apiVersion: apps/v1
kind: Deployment
metadata:
  name: workshop-service
  labels:
    app: workshop-service
spec:
  selector:
    matchLabels:
      app: workshop-service
  replicas: 2
  template:
    metadata:
      labels:
        app: workshop-service
    spec:
      containers:
        - name: workshop-service
          image: europe-west4-docker.pkg.dev/workshop-307013/docker-repository/workshop-us63:1.6.0
          imagePullPolicy: Always
          ports:
            - containerPort: 8080